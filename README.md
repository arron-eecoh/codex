# The Steward's Codex
EECOH Living's RPG habit tracker. Single-file app (`index.html`) + Netlify function (`netlify/functions/ai.js`) proxying Claude.
- Tests: `node codex-harness.js` (all suites must pass)
- Rules for AI-assisted dev: see `CLAUDE.md`
