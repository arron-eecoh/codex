# Update log
- 2026-07-18 — LIVE LAUNCH build (r454+cloud): Supabase accounts (magic-link via Resend SMTP), local-first cloud sync (rev-based merge, debounced push, RLS row security), JWT-gated Claude proxy, env-driven client config, Settings Account & Sync panel. 468/468 across 18 suites.
- 2026-07-18 — Initial release feature set: bottom thumb-zone nav (frequency-ramped), sub-tab dock, docked timer + cross-tab pill, occlusion-aware auto-navigation, Forge voice + hands-free, interactive warm-up with per-side holds, fill-forward, coordination engine, skill ladders, handedness mirroring, density modes, modal ergonomics standard, build stamps.
- 2026-07-31 — PWA (r455): web manifest (standalone, portrait, themed #0e1411), seedling-forge app icons (192/512/maskable + apple-touch), head wiring (manifest link, theme-color, iOS/Android standalone metas) — home-screen shortcut now launches fullscreen like a native app. New pwa.test.js suite guards manifest presence, head wiring, and icon assets.
- 2026-07-31 — Session feel sliders (r456): post-workout 5-star rating replaced with two 0–5 sliders — 🔥 Body and 🧠 Mind effort — so every session logs how it felt physically AND mentally (6 settings each, worded effortless→maxed out) for future routine analysis. Sessions store feelPhys/feelMind; legacy rating field mirrors Body for old clients, old starred sessions still show stars in the dashboard. Voice finish prompt and auto-navigation updated; autonav + reachability suites cover the new controls.
- 2026-08-03 — Alchemy AI fix (r457): text/photo/menu estimates and the coach no longer die with the stale "AI needs to run inside Claude" message on the hosted build. Real causes now surface via aiFailMsg — a 🔑 sign-in prompt when the Supabase session is missing/expired, the actual proxy/API error otherwise, and a network hint as the fallback. Web-search tool upgraded to web_search_20260209 (the 20250305 variant predates claude-sonnet-4-6 and could fail the "look up unknown foods" path). Consistency suite guards all three.
- 2026-08-03 — Photo AI tuned up (r458): 📷 plate photos (macro estimation) and 📜 menu scans (best-pick curation) confirmed working end-to-end through the fixed proxy. Menu curation now also sees TODAY'S TRAINING context (targets + refuel-vs-lean steering), and photos upload at 1568px — the model's vision cap — instead of 1280px, so small menu text stays legible. Consistency suite now guards both pipelines, the training context, and the resolution.
- 2026-08-03 — Update nudge (r459): long-lived pages (open tabs, installed PWA resumed from memory) now learn when a new build ships. A tiny build.json is published beside the app; the page polls it when you return to the app (throttled to 5 min) and hourly, and shows a dismissible "✨ Update ready — tap to refresh" banner when the stamps differ. Fully passive (never auto-reloads), silent offline. Consistency suite pins build.json to the BUILD stamp so the deploy gate keeps them in sync forever.
- 2026-08-06 — Camera fast-path (r460): 📷 Snap a meal and 📜 Scan a menu now open the phone camera directly (capture="environment" inputs); new 🖼 buttons beside each keep gallery/screenshot upload (delivery-app menus etc.). All four inputs feed the same AI pipelines; buttons disable together while reading. Note: the "credit balance too low" error on text/photo/menu is the Anthropic API account needing a top-up (platform.claude.com → Plans & Billing), not an app bug — all three share one pipeline and recover together once funded.
- 2026-08-09 — Travel split + monthly rotation (r461): travel mode now has six DISTINCT days (Push A/B and Pull A/B were silently identical). Every travel exercise is strictly bodyweight or a band you stand on / hold — anchored moves (banded pulldown/row on an anchor, dead hang, pull-ups, Leg B's Pallof press) replaced with unanchored equivalents. NEW: monthly exercise rotation — every slot (gym and travel) has a same-pattern alternate, and the whole program flips between Rotation A and Rotation B on the 1st of each month (deterministic from the date; picker shows which rotation is live). Progression history per exercise is preserved and returns when its month comes back. New travel-rotation suite guards day distinctness, the no-anchor rule (including all alternates), duplicate-free days in both rotations, and date-driven determinism.
- 2026-08-09 — Four-month rotation (r462): rotation expanded from A/B to A/B/C/D — every exercise slot (gym and travel) now carries three same-pattern alternates, and the whole program cycles through four distinct routines month by month before recycling. Picker shows "Rotation X of A-D". Travel alternates all honor the no-anchor rule; travel-rotation suite now asserts 3 alternates per pool and duplicate-free days across all four rotations; harness clock pinned to a %4 Rotation-A month.
- 2026-08-10 — Every exercise gets its ⓘ (r463): authored how-to descriptions for all 217 exercises that lacked one — every base exercise and every rotation alternate across gym and travel programs (272 total) now has the expandable "ⓘ how-to" coaching text. HARDWIRED: the travel-rotation suite now fails the build if any exercise in any day or rotation ships without a real EX_INFO entry (≥60 chars), so no future month, cycle, or program edit can produce a workout without its description.
- 2026-08-10 — Earned progression gates (r464): advanced rotation alternates (wall HSPU negatives, L-sit tuck hold, pseudo planche, shrimp squat, weighted hangs, and a dozen more) no longer appear until YOUR logged history proves readiness — the gate needs multiple sessions of the prerequisite exercise with every checked set at the target reps, none of them rated 🔥5 "maxed out" on the post-workout Body slider. Until then the slot serves its familiar base with a "🔒 <exercise> unlocks as you build strength" note, and the advanced variant trickles in on its next rotation month once earned. Applies to every lift and travel day through the shared day-builder. Suite guards: no dead gates, real prerequisites, no alt shadowing another slot's base, and lock/unlock/maxed-out behavior from seeded log data.
- 2026-08-10 — 👎 dislike button (r465): every exercise card now has a 👎 beside the ⓘ. Tapping it (with confirm) permanently bans that exercise, instantly swaps the slot to the next eligible alternative from its family (rotation pick → base → remaining alternates, still respecting strength gates), and the ban lives in synced state so it never appears again on any device, in any rotation, ever. Dislike the whole family and the slot clears. This is the preference layer: the program dials into what the user actually likes over time. Suite covers state-level resolution order, whole-family drops, and the live tap-swap flow.
- 2026-08-11 — 🧪 Test AI connection (r466): one tap in the Alchemy pane diagnoses the entire AI chain and names the exact fix. The Netlify function gained an auth-gated deep check (?check=1) that probes Anthropic server-side with a ~1-token call, so verdicts are ground truth: server unreachable / ANTHROPIC_API_KEY missing in Netlify / signed out / sign-in expired / key rejected by Anthropic / key valid but ITS account has no credits (with the per-account explanation) / all-clear. Ends the screenshot-guessing loop for the text, photo, and menu estimate features. Consistency suite mocks every layer failure and asserts each verdict.
